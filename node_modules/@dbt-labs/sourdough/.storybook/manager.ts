import { addons } from '@storybook/manager-api';
import { create } from '@storybook/theming/create';

addons.setConfig({
  theme: create({
    base: 'dark',
    brandTitle: 'dbt UI',
    brandUrl: 'https://getdbt.com',
    brandImage: 'https://docs.getdbt.com/img/dbt-logo-light.svg',
  }),
});
