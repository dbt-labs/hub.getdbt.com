import type { Config } from '@svgr/core';
import { type Logger } from '../helpers.internal';
type SVGMetadata = {
    /** name of component without extension e.g. RyeconAccount */
    ryeconName: string;
    /** name of component with extension e.g. RyeconAccount.tsx */
    ryeconTsx: string;
    /** name of asset without extension e.g. account */
    assetName: string;
    /** name of asset with extension e.g. account.svg */
    assetSvg: string;
    /** full path to svg asset */
    assetPath: string;
    /** assetDir of svg asset */
    assetDir: string;
};
/**
 * builds required metadata needed for generating new components
 */
export declare const getSvgMetadata: (svgFileNameList: string[], assetDir: string) => SVGMetadata[];
export type ProcessSVGsParams = {
    isDryRun: boolean;
    singleSvgPath: string | boolean;
    assetDir: string;
    componentDir: string;
    exportsFilePath: string;
    logger: Logger;
    svgoConfig: Config['svgoConfig'];
    customTemplate: Config['template'];
};
/**
 * 1. get the list of all svgs in `${ryeconDir}/svgs`
 * 2. generate a react component jsx for each one of them
 * 3. generate an `index` file exporting each Icon
 */
export declare const processSVGs: ({ isDryRun, singleSvgPath, assetDir, componentDir, exportsFilePath, logger, svgoConfig, customTemplate, }: ProcessSVGsParams) => Promise<{
    deleted: string[];
    generated: string[];
    indexContent: string;
}>;
export {};
//# sourceMappingURL=generate.internal.d.ts.map