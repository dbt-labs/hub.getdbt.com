import { ButtonStyleType } from '../Button';
import { Sizes } from '../types';
export interface CopyButtonProps {
    /** label text of the input where the copied text comes from */
    inputLabel?: string;
    /** The text to copy when clicked */
    text: string;
    /** Custom classnames */
    className?: string;
    /** The Copy Button style type */
    type?: ButtonStyleType;
    /** The button size */
    size?: Sizes;
    /** data-testid for cypress targeting */
    testId?: string;
    /** data-trackingid for pendo targeting */
    trackingId?: string;
}
export declare const CopyButton: React.FC<CopyButtonProps>;
//# sourceMappingURL=CopyButton.d.ts.map