import { ReactElement } from 'react';
import type { Placement } from '@floating-ui/react';
import { ButtonProps } from '../Button';
import { SaveButtonProps as SaveAction } from '../SaveButton';
import { Sizes } from '../types';
export declare const isSaveButton: (action: ActionType) => action is SaveAction;
export declare const isCustomAction: (action: ActionType) => action is ReactElement;
type ActionType = Action | SaveAction | ReactElement;
export type ActionList = ActionType[];
export interface Action {
    text: React.ReactNode;
    href?: string;
    onClick?(event: React.MouseEvent): void;
    buttonProps?: Partial<ButtonProps>;
    tooltip?: string;
    /**
     * Controls where a tooltip is placed in relation to the target.
     * If not provided, tooltip will default to `autoPlacement`
     */
    tooltipPlacement?: Placement;
}
export interface ActionsProps {
    actions: ActionList;
    /** Size of the action buttons */
    size?: Sizes;
}
export declare const Actions: React.FC<ActionsProps>;
export {};
//# sourceMappingURL=Actions.d.ts.map