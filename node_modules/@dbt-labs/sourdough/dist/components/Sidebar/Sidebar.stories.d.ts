import type { Meta } from '@storybook/react/*';
import { Sidebar } from './Sidebar';
/**
 * The `Sidebar` handles nav listings for settings pages.
 *
 * Each `SidebarSection` contains a group of related items which will exist together on a scrollable page.
 *
 * Each `SidebarItem` can link to an element on the page, or take a custom onClick handler.
 *
 * When a `itemId` is passed, clicking the sidebar item will navigate to the top level route in that section,
 * and scroll the page to the given item id. For example, passing `projects` as the itemId in a section with route `/settings`,
 * will navigate to /settings and then scroll to the top of the element with the id `projects`.
 *
 * When an `onClick` handler is passed, clicking the sidebar item will trigger the onClick. This is used for special actions such as
 * "Sign out", and will take precedence over the itemId.
 *
 * When a `route` prop is passed, the SidebarItem will act as a standard link tag.
 */
declare const meta: Meta<typeof Sidebar>;
export default meta;
/**
 * Note: Behavior in storybook is a little weird given the non standard routing setup.
 * For a more fleshed out example, check out how the sidebar is used in `cloud-ui`
 */
export declare const Basic: () => import("react/jsx-runtime").JSX.Element;
export declare const Loading: () => import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=Sidebar.stories.d.ts.map