import { To } from 'react-router-dom';
import { EmptyStateProps } from '../../EmptyState/EmptyState';
import { PaginationFooterProps } from '../../PaginationFooter/PaginationFooter';
import { Cell, CommonTableProps, DefaultTData, StickyColumn } from '../types';
import type { SortableTableProps } from '../types.internal';
/**
 * Props specific to Table
 */
type TableOnlyProps<TData> = {
    /** The height (in pixels) of the rows in the table. Required for virtualization along with `paginationProps` */
    rowHeight?: number;
    /** Configuration for EmptyState, displayed when no rows are provided */
    emptyStateProps?: EmptyStateProps;
    /** Configuration for the pagination footer, required for virtualization */
    paginationProps?: PaginationFooterProps;
    /**
     * Callback to generate a `react-router` link to navigate to on row click.
     * Mutually exclusive with onRowClickCustomHandler.
     * Uses `<Link />` behind the scenes and should be used when you want to enable right click -> open in new tab for a row
     */
    onRowClickLinkGenerator?(data: TData): To;
    /**
     * Can be used to specify whether the row-level link generated by onRowClickLinkGenerator is internal.
     * Defaults to `true`
     */
    rowLinkIsInternal?: boolean;
    /**
     * Custom callback to handle row click events.
     * Mutually exclusive with onRowClickLinkGenerator
     * onRowClickLinkGenerator should be preferred.
     * Should be used only when you require custom click handling.
     */
    onRowClickCustomHandler?(data: TData): void;
    /** Handle isCellClickable for custom click handling */
    isCellClickable?: (cell: Cell<TData, unknown>) => boolean;
    /** Handle cell click for custom click handling */
    onCellClick?: (cell: Cell<TData, unknown>) => void;
    /** Callback to handle adding a new row */
    onAddRow?(): void;
    /** Label for the add row button */
    addRowLabel?: string;
    /** Flag to disable (but not hide) add button (isDisabled) */
    isAddButtonDisabled?: boolean;
    /** Determines whether the first or last column should be sticky if the table is wider than its container -- only use if you really need a column to stay visible if the container is too narrow for the table! */
    stickyColumn?: StickyColumn;
    /** data-testid for cypress targeting */
    testId?: string;
    /** data-trackingid for pendo targeting */
    trackingId?: string;
    /** Custom classes for the table */
    className?: string;
    /**
     * Flag to enable virtualization.
     * if rowHeight and paginationProps exist, this will be auto enabled
     * if paginationProps are not present, `height` and `rowHeight` are required for this to work
     */
    isWindowed?: boolean;
    /** Static height (in px), for locking the table to a specific height */
    height?: number;
};
export type TableProps<TData extends DefaultTData = DefaultTData, TValue extends React.ReactNode = React.ReactNode> = CommonTableProps<TData, TValue> & TableOnlyProps<TData> & SortableTableProps;
export {};
//# sourceMappingURL=types.d.ts.map