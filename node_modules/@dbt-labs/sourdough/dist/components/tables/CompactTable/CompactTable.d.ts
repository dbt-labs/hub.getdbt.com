import { type ReactNode } from 'react';
import { Cell } from '@tanstack/react-table';
import { CommonTableProps, DefaultTData } from '../types';
import type { SortableTableProps } from '../types.internal';
export type SelectionState = {
    [key: string]: boolean;
};
type CompactOnlyProps<TData> = {
    rowHeight: number;
    height: number;
    /** # of rows to load into DOM beyond scroll container */
    overscanCount?: number;
    enableCellSelection?: boolean | ((cell: Cell<TData, unknown>) => boolean);
    cellSelection?: SelectionState;
    onCellClick?: (cell: Cell<TData, unknown>, isSelected: boolean) => void;
    /** # of columns (and their cells) to load into DOM beyond scroll container */
    columnOverscanCount?: number;
    control?: CompactTableControl;
};
export type CompactTableProps<TData extends DefaultTData, TValue extends React.ReactNode = React.ReactNode> = CommonTableProps<TData, TValue> & CompactOnlyProps<TData> & SortableTableProps;
export declare const CompactTable: <TData extends DefaultTData, TValue extends React.ReactNode = ReactNode>({ columns, data, emptyStateProps, loadingRowCount, isLoading, rowHeight, testId, trackingId, className, height, overscanCount, columnOverscanCount, enableCellSelection, cellSelection, onCellClick, cellVariant, isSortable, initialSortColumn, initialSortDesc, onChangeSort, control, }: CompactTableProps<TData, TValue>) => React.ReactElement;
type ScrollAlignment = 'start' | 'center' | 'end' | 'auto';
type ScrollBehavior = 'auto' | 'smooth';
type ScrollToOptions = {
    align?: ScrollAlignment;
    behavior?: ScrollBehavior;
};
export declare const useCompactTableControl: () => {
    ref: import("react").RefObject<{
        scrollToColumnIndex: (index: number, options?: ScrollToOptions) => void;
    } | null>;
    scrollToColumnIndex: (index: number, options?: ScrollToOptions) => void;
};
export type CompactTableControl = ReturnType<typeof useCompactTableControl>;
export {};
//# sourceMappingURL=CompactTable.d.ts.map