import { type ReactNode } from 'react';
import type { DefaultTData } from '../types';
import type { InternalTableProps } from '../types.internal';
type VirtualTableProps<TData extends DefaultTData, TValue extends React.ReactNode> = InternalTableProps<TData, TValue> & {
    rowHeight: number;
};
/**
 * Internal table that handles virtualized use cases.
 * Similar to CompactTable but with the same feature set as Table.
 * We may be able to deprecate CompactTable in the future if this is sufficiently flexible.
 *
 * Note: this is not exported so it cannot be used directly by consumers
 */
export declare const VirtualTable: <TData extends DefaultTData, TValue extends React.ReactNode = ReactNode>({ columns, rows, headers, emptyStateProps, rowHeight, paginationProps, handleRowClick, onRowClickLinkGenerator, rowLinkIsInternal, stickyColumn, testId, trackingId, onAddRow, addRowLabel, isAddButtonDisabled, height, cellVariant, className, showEmpty, showPaginationFooter, tableContainerRef, onCellClick, isCellClickable, }: VirtualTableProps<TData, TValue>) => import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=VirtualTable.d.ts.map