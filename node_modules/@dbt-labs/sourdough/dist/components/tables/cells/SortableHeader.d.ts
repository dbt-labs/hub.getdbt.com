import { type Header, type SortDirection } from '@tanstack/react-table';
import { type DefaultTData } from '../types';
type SortableHeaderProps<TData extends DefaultTData, TValue extends React.ReactNode> = {
    header: Header<TData, TValue>;
    onClick?: (event: unknown) => void;
    sortDir: false | SortDirection;
    isRightAligned: boolean;
    isSorted: boolean;
};
export declare const SortableHeader: <TData extends DefaultTData, TValue extends React.ReactNode>({ header, onClick, sortDir, isRightAligned, isSorted, }: SortableHeaderProps<TData, TValue>) => import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=SortableHeader.d.ts.map