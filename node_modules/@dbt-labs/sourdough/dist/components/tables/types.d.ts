/**
 * types shared between Table & CompactTable
 */
import { Cell as RTCell, ColumnDef as RTColumnDef, Row as RTRow, RowData as RTRowData } from '@tanstack/react-table';
import { EmptyStateProps } from '../EmptyState/EmptyState';
export type { Cell, CellContext, Column, ColumnDef, ColumnSort, createColumnHelper, Header, HeaderContext, HeaderGroup, Row, RowData, SortingState, TableOptions, } from '@tanstack/react-table';
export type StickyColumn = 'first' | 'last';
export type DefaultTData = object & {
    isLoadingRow?: boolean;
    testId?: string;
};
export type DefaultRowProps = RTRowData & {
    isLoadingRow?: boolean;
};
export type CellVariant = 'DEFAULT' | 'COMPACT' | 'NO_PADDING';
export type CellProps<TData extends DefaultTData = DefaultTData, TValue extends React.ReactNode = React.ReactNode> = {
    row: RTRow<TData>;
    cell: RTCell<TData, TValue>;
    index: number;
    stickyColumn?: StickyColumn;
    testId?: string;
    trackingId?: string;
    cellVariant?: CellVariant;
};
/**
 * for default Cell renderer usage
 */
export type CellRenderer<TData extends DefaultTData = DefaultTData, TValue extends React.ReactNode = React.ReactNode> = React.FC<CellProps<TData, TValue>>;
export type CommonTableProps<TData, TValue> = {
    /** A react-table column definition array */
    columns: RTColumnDef<TData, TValue>[];
    /** Data for the table's rows */
    data: TData[];
    /** A flag to indicate a full load of the table contents */
    isLoading?: boolean;
    /** The number of loading rows to display when isLoading is `true` */
    loadingRowCount?: number;
    /** data-testid for cypress targeting */
    testId?: string;
    /** data-trackingid for pendo targeting */
    trackingId?: string;
    /** Custom classes for the table/compact table */
    className?: string;
    /** Custom cell variant for table cell style */
    cellVariant?: CellVariant;
    /** Configuration for EmptyState, displayed when no rows are provided */
    emptyStateProps?: EmptyStateProps;
};
//# sourceMappingURL=types.d.ts.map