import { Meta } from '@storybook/react';
import { ContextMenu } from './ContextMenu';
/**
 * A `ContextMenu` is another name for the Right Click Menu. It opens a list of
 * menu items that can be interacted with, it can be portaled to higher in the DOM,
 * and it manages user focus.
 */
declare const meta: Meta<typeof ContextMenu>;
export default meta;
export declare const Basic: () => import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=ContextMenu.stories.d.ts.map