import React from 'react';
import { LinkProps, NavLinkProps } from 'react-router-dom';
import { ButtonStyleType } from '../Button/buttonClassNames.internal';
import { Sizes } from '../types';
interface LinkOrAnchorBase {
    /** To be used when linking to other pages within the same app, uses a react-router Link, with our classes applied. */
    isInternal: boolean;
    shouldOpenNewTab?: boolean;
    testId?: string;
    trackingId?: string;
    children: React.ReactNode;
    hideUnderline?: boolean;
    /** special use-cases; see stories; overridden if you declare a buttonStyleType */
    colorVariant?: 'default' | 'headless' | 'success' | 'error' | 'warning' | 'info' | 'tooltip' /** @deprecated. We now use tokens to handle dark background SURF-1569 */ | 'light' /** @deprecated. We now use tokens to handle dark background SURF-1569 */ | 'info';
    onClick?(e: React.MouseEvent<HTMLAnchorElement>): void;
    /** Use if you need a link to look like a button; overrides colorVariant styles */
    buttonStyleType?: ButtonStyleType;
    /** Use with buttonStyleType */
    buttonIsDisabled?: boolean;
    /** Use with buttonStyleType */
    buttonIsRounded?: boolean;
    /** Use with buttonStyleType */
    buttonSize?: Sizes;
}
interface InternalLink extends LinkProps {
    activeClassName?: never;
    type?: 'link';
    className?: string;
}
interface InternalNavLink extends NavLinkProps {
    type?: 'navlink';
    className?: string;
    /** If you need additional classNames that only exist when a nav link is active, put them here. */
    activeClassName?: string;
}
interface ExternalLink {
    activeClassName?: never;
    className?: string;
    to: string;
    type?: 'link';
}
export type LinkOrAnchorProps = LinkOrAnchorBase & (InternalLink | InternalNavLink | ExternalLink);
export declare const Link: React.ForwardRefExoticComponent<LinkOrAnchorProps>;
export {};
//# sourceMappingURL=Link.d.ts.map