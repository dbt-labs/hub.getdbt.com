import { PaginationProps } from './Pagination';
/**
 * Internal pagination state.  Determines if the back and next buttons are disabled, what the currentPage is, and provides page change functions.
 */
declare const usePaginationInternal: ({ currentPage, onPageChange, rowsPerPage, totalRows, }: PaginationProps) => {
    backButtonDisabled: boolean;
    currentPage: number;
    doPageBack: () => void;
    doPageForward: () => void;
    goToPage: (page: number) => () => void;
    nextButtonDisabled: boolean;
    pageNumbersToShow: number[];
    totalPages: number;
};
export default usePaginationInternal;
//# sourceMappingURL=usePagination.internal.d.ts.map