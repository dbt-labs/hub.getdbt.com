/**
 * Pull the transform attribute from an element
 *
 * @deprecated
 * @param el element with the transform attribute
 * @returns transform attribute - translate(x, y)
 */
export declare const getTransformAttribute: (el: JQuery<HTMLElement>) => Cypress.Chainable<string | undefined>;
/**
 * Pull the amount of translation from the transform attribute in x or y
 *
 * @deprecated
 * @param transform transform attribute that contains translate(x, y)
 * @param isX are we looking for x or y of the translation
 * @returns value of the translation in x or y
 */
export declare const getTransformValue: (transform: string | undefined, isX: boolean) => number;
/**
 * Calculate the relative x value compared to a reference x value.
 * If x axis scale does not start at 0 startScale is required
 *
 * @deprecated
 * @param x value of object being compared
 * @param referenceX y value of the reference
 * @param xStartScale where your x axis scale starts
 * @returns
 */
export declare const calculateRelativeSizeX: (x: number, referenceX: number, xStartScale?: number) => number;
/**
 * Calculate the relative value compared to a reference value.
 * If y axis scale does not start at 0 startScale is required
 *
 * @deprecated
 * @param y y value of object being compared
 * @param referenceY y value of the reference
 * @param endScale where your y axis scale ends
 * @returns
 */
export declare const calculateRelativeSizeY: (y: number, referenceY: number, yEndScale?: number) => number;
/**
 * Mouse over an interaction layer of a chart to show it's corresponding tooltip
 *
 * @deprecated
 * @param interactionLayerTestId id of the interaction layer
 * @param index if multiple interaction layers exist, which one to interact with
 */
export declare const showTooltip: (interactionLayerTestId: string, index?: number, force?: boolean) => void;
/**
 * Mouse away from an interaction layer of a chart to hide it's corresponding tooltip
 *
 * @deprecated
 * @param interactionLayerTestId id of the interaction layer
 * @param index if multiple interaction layers exist, which one to interact with
 */
export declare const hideTooltip: (interactionLayerTestId: string, index?: number, position?: string, force?: boolean) => void;
/**
 * Get the tooltip element
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @returns tooltip element
 */
export declare const getTooltip: (chartTestId: string) => Cypress.Chainable<JQuery<HTMLElement>>;
/**
 * Get the testId of the axis
 *
 * @deprecated
 * @param isXAxis is the axis being tested on the x plane
 * @param axisType what type of axis is it (category, linear, time, etc)
 * @returns testid of the axis
 */
export declare const getAxisTestId: (isXAxis: boolean, axisType: string) => string;
/**
 * Check that the category axis is rendered correctly
 *
 * @deprecated
 * @param isXAxis is the axis being tested on the x plane
 * @param axisLabel axis name if set
 * @param labels list of labels that should be on the axis
 */
export declare const isCategoryAxis: (isXAxis: boolean, axisLabel: string, labels: string[]) => void;
/**
 * Check that the linear axis is rendered correctly
 *
 * @deprecated
 * @param isXAxis is the axis being tested on the x plane
 * @param axisLabel axis name if set
 * @param min minimum value of the linear axis that is shown on the axis
 * @param max maximum value of the linear axis that is shown on the axis
 */
export declare const isLinearAxis: (isXAxis: boolean, axisLabel: string, min: number | string, max: number | string) => void;
/**
 * Get the location (x or y) of each tick in the axis
 *
 * @deprecated
 * @param axisTestId testId of the <Axis/> component
 * @param isHorizontal is the axis for the x plane
 * @returns list of tick locations (x or y) along a single axis
 */
export declare const getVisibleTickLocations: (axisTestId: string, isHorizontal?: boolean) => number[];
/**
 *
 * Compare the relative size of all bars in a bar chart to determine whether
 * the bars are sized correctly to the data.
 * A reference bar is required to compare the relative size of all
 * other bars, this is set using an index relative to how the bars are rendered
 * left to right or bottom to top depending on the bar chart orientation.
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @param relativeSizesToReference list of relative sizes
 * (compared to the bar at referenceIndex) for each bar
 * ie [1, 3, 2, 4] or [1/2, 1/5, 65/88, 2]
 * @param referenceIndex index reference to the bar that is [1] in relativeSizesToReference
 * * ie 0
 * @param isHorizontal is the bar chart horizontal
 */
export declare const compareRelativeBar: (chartTestId: string, relativeSizesToReference: number[], referenceIndex?: number, isHorizontal?: boolean) => void;
/**
 * Get the location (x or y) of each bar in the bar chart along the categorical axis.
 *
 * @deprecated
 * @param plotTestId testId of the <Plot/> component
 * @param isHorizontal is the bar chart horizontal
 * @returns list of bar locations (x or y)
 */
export declare const getBarLocations: (plotTestId: string, isHorizontal?: boolean) => number[];
/**
 * Check whether the tick and bars have the same distance between each other
 * Allows us to check whether the data is matched to the correct bar
 *
 * If the chart is not set up correctly this test will fail due to the bars being all set at 0
 *
 * @deprecated
 * @param tickLocations list of tick locations (x or y) along a single axis
 * @param barLocations list of bar locations (x or y) along a single axis
 */
export declare const areTicksAndBarsEvenlySpaced: (tickLocations: number[], barLocations: number[]) => void;
/**
 * Compare the relative size of all bars in a complex bar chart to determine whether
 * the bars are sized correctly to the data.
 * A reference bar is required to compare the relative size of all
 * other bars, this is set using an index relative to how the bars are rendered.
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @param barStyleTestId testId of the <Shape/> component that renders the multiple bars (BarGroup, BarStack)
 * @param relativeSizesToReference
 * [[2/3, 1/2], [1, 1/22], [4/21, 5/11]]
 * @param referenceGroupIndex index reference to the group of bars that is [1] in relativeSizesToReference
 * ie 1
 * @param referenceIndex index reference to the bar that is [1] in relativeSizesToReference
 * ie 0
 * @param isHorizontal is the bar chart horizontal
 */
export declare const compareRelativeBarValues: (chartTestId: string, barStyleTestId: string, relativeSizesToReference: number[][], referenceGroupIndex?: number, referenceIndex?: number, isHorizontal?: boolean) => void;
/**
 * Test whether the bars in a group/stack bar chart are colored correctly.
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @param barStyleTestId testId of the <Shape/> component that renders the multiple bars (BarGroup, BarStack)TestId
 * @param colors ordered list of color classnames that we expect the bars to be colored
 */
export declare const isBarsColored: (chartTestId: string, barStyleTestId: string, colors: string[]) => void;
/**
 * Compare the relative size of all bars in a group bar chart to determine whether
 * the bars are sized correctly to the data.
 * A reference bar is required to compare the relative size of all
 * other bars, this is set using an index relative to how the bars are rendered.
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @param relativeSizesToReference
 * [[2/3, 1/2], [1, 1/22], [4/21, 5/11]]
 * @param referenceGroupIndex index reference to the group of bars that is [1] in relativeSizesToReference
 * ie 1
 * @param referenceIndex index reference to the bar that is [1] in relativeSizesToReference
 * ie 0
 * @param isHorizontal is the bar chart horizontal
 */
export declare const compareRelativeBarGroupBarValues: (chartTestId: string, relativeSizesToReference: number[][], referenceGroupIndex?: number, referenceIndex?: number, isHorizontal?: boolean) => void;
/**
 * Check that bar group bars are colored correctly
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @param colors ordered list of color classnames that we expect the bars to be colored
 */
export declare const isBarGroupColored: (chartTestId: string, colors: string[]) => void;
/**
 * Check if specific bar is colored correctly
 *
 * @deprecated
 * @param barTestId
 * @param color
 */
export declare const isBarInBarGroupColored: (barTestId: string, color: string) => void;
/**
 * Check that the bars in a group bar chart are not overlapping each other
 *
 * @deprecated
 * @param positions positions of bars, organized by group
 * @param sizes size of bars, organized by group
 */
export declare const isBarGroupNotOverlapping: (positions: number[][], sizes: number[][]) => void;
/**
 * Get all locations of the group relative to the category axis
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @param isHorizontal is bar horizontal
 * @returns list of locations on the axis with categories
 */
export declare const getGroupBarLocations: (chartTestId: string, isHorizontal?: boolean) => number[];
/**
 * Get value of a specific attribute on all bars within a bar group chart
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @param attr name of attribute to pull from the bar
 * @returns list of attribute values in order of how the bars are rendered in groups
 */
export declare const getBarGroupBarAttribute: (chartTestId: string, attr: string) => number[][];
/**
 * Check whether the bars in a group bar chart are evenly spaced between each other
 *
 * @deprecated
 * @param barLocations list of bar locations (x or y) along a single axis
 */
export declare const areBarGroupBarsEvenlySpaced: (barLocations: number[][]) => void;
/**
 * Compare the relative size of all bars in a stacked bar chart to determine whether
 * the bars are sized correctly to the data.
 * A reference bar is required to compare the relative size of all
 * other bars, this is set using an index relative to how the bars are rendered.
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @param relativeSizesToReference
 * [[2/3, 1/2], [1, 1/22], [4/21, 5/11]]
 * @param referenceGroupIndex index reference to the group of bars that is [1] in relativeSizesToReference
 * ie 1
 * @param referenceIndex index reference to the bar that is [1] in relativeSizesToReference
 * ie 0
 * @param isHorizontal is the bar chart horizontal
 */
export declare const compareRelativeBarStackBarValues: (chartTestId: string, relativeSizesToReference: number[][], referenceGroupIndex?: number, referenceIndex?: number, isHorizontal?: boolean) => void;
/**
 * Check whether the bars in a stacked bar chart are colored correctly.
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @param colors ordered list of color classnames that we expect the bars to be colored
 */
export declare const isBarStackColored: (chartTestId: string, colors: string[]) => void;
/**
 * From a list of x positions for the rendered dots, compare the relative position of each dot in the x axis
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @param xPositions list of x axis values for each dot
 * @param referenceIndex index reference to the dot that is not equal to 0 in xPositions
 * @param marginLeft your left margin value
 * @param xScaleStart the start of your linear scale in the x axis
 */
export declare const compareRelativeDotX: (chartTestId: string, xPositions: number[], referenceIndex: number, marginLeft: number, xScaleStart: number) => void;
/**
 * From a list of y positions for the rendered dots, compare the relative position of each dot in the y axis
 *
 * @deprecated
 * @param chartTestId testId of the <Chart/> component
 * @param yPositions list of y axis values for each dot
 * @param referenceIndex index reference to the dot that is not equal to 0 in xPositions
 * @param marginTop your top margin value
 * @param yScaleEnd the end of your linear scale in the y axis
 */
export declare const compareRelativeDotY: (chartTestId: string, yPositions: number[], referenceIndex: number, marginTop: number, yScaleEnd: number) => void;
//# sourceMappingURL=chartTestUtils.d.ts.map