import { MenuItem } from '../../sourdough';
import { BadgeType } from '../Badge';
export interface Breadcrumb {
    id?: string;
    text: string;
    href?: string;
    icon?: string;
    badge?: string;
    badgeType?: BadgeType;
    dropdown?: {
        className?: string;
        items: MenuItem[];
        onClick?: (item: string | number, event: React.MouseEvent) => void;
    };
    skeleton?: boolean;
    'aria-current'?: 'page' | 'step' | 'location' | 'date' | 'time';
}
export interface BreadcrumbsProps {
    /**
     * Array of breadcrumb items to display
     */
    breadcrumbs: Breadcrumb[];
    /**
     * Optional callback when a breadcrumb item is clicked
     */
    onItemClick?: (item: Breadcrumb) => void;
    /**
     * Optional aria-label for the breadcrumb navigation
     * @default 'Breadcrumb'
     */
    'aria-label'?: string;
    /**
     * Custom classes to apply to the component
     */
    className?: string;
}
//# sourceMappingURL=types.d.ts.map