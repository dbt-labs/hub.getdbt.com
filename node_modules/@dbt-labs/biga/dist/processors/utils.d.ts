import { type TransformedToken } from 'style-dictionary';
import { Mode, type TokensByMode } from '../types.js';
export declare const splitModes: (tokens: TransformedToken[], modes: Mode[]) => TokensByMode;
/**
 * will need to extend this later for things like `number` or `typography`
 */
export declare const getTokenName: (token: TransformedToken, mode: Mode) => string;
/**
 * will need to extend this in future to do things like grabbing RGB values from token.attributes
 */
export declare const getTokenValue: (token: TransformedToken) => any;
export declare const sortByName: (tokens: TransformedToken[], mode: Mode) => TransformedToken[];
//# sourceMappingURL=utils.d.ts.map