type ParserOptions = {
    filePath?: string;
    contents?: string;
};
/**
 * there are some oddities about what the figma exports like how the variables are named
 * e.g. primitives v.s. color primitives
 * this attempts to smooth those out
 */
export declare const parseAssets: ({ filePath, contents }: ParserOptions) => any;
export {};
//# sourceMappingURL=parseAssets.d.ts.map